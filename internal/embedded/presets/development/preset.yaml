name: development
description: Go and Node conventions — project layout, modern idioms, testing, CLI, backend, frontend, concurrency, CI/CD, README, plus the develop entry point and the review-code audit
